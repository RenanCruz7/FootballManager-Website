import { IdadeValidaDirective } from './idade-valida.directive';

describe('IdadeValidaDirective', () => {
  it('should create an instance', () => {
    const directive = new IdadeValidaDirective();
    expect(directive).toBeTruthy();
  });
});
